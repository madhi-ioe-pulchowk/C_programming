// C program to use getchar() and putchar()

#include <stdio.h>
#include <stdlib.h>

int main()
{
    // read a character and stores it in ch using getchar()
    char ch = getchar();
    // displays the character using putchar()
    putchar(ch);
    system("pause");
    return 0;
}
